name: Hello World Workflow

on:
  push:
   branches:
     -main

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Hello Shivakumar"

      - name: Print Date
        run: date

      - name: Print Current Directory
        run: pwd

      - name: List Files
        run: ls -ltr
